<%=
select_tag @tag_id, options_from_collection_for_select(DataElementCollection.find_top_level_data_elements(@fk_class_name), "id", viewable_column_name, @default_id), :include_blank => @default_id.nil? ? true : false, :class => "picklist_foreign_key", :onchange => ("javascript:submit();"), :name => @tag_name
%>