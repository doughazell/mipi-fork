<!DOCTYPE html>
<html>
<head>
  <title>BUDAMAN! Alpha</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag "dhtmlwindow" %>
  <%= csrf_meta_tag %>
  <%= if !@custom_stylesheet.blank? then stylesheet_link_tag @custom_stylesheet end %>
  <%= yield :head %>
</head>
<body>
<div id="user_nav" class="user_nav">
  <% if user_signed_in? %>
  Signed in as <%= current_user.email %>. Not you? <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
  <% else %>
  <%= link_to "Sign Up", new_user_registration_path %> or <%= link_to "Sign In", new_user_session_path %>
  <% end %>
</div>
<% if notice.present? then %>
  <p class="login_notice"><%= notice %></p>
<% end %>
<% if alert.present? then %>
  <p class="login_alert"><%= alert %></p>
<% end %>
<%= yield %>
<%= render "/the_bottom_line" %>
</body>
</html>
